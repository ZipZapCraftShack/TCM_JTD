# .gitignore for Jekyll + Bundler Projects
# -----------------------------------------
# This file tells Git which files and folders to ignore in version control.
# Keeping unnecessary files out of your repository improves maintainability and reduces clutter.
# For more info: https://git-scm.com/docs/gitignore

# Jekyll build output and caches (do not commit generated site or build caches)
_site/           # Jekyll's generated site output
.sass-cache/     # SASS cache folder (deprecated in newer SASS, but safe to ignore)
.jekyll-cache/   # Jekyll's cache folder (improves build speed)
.jekyll-metadata # Jekyll's build metadata file

# Bundler and Ruby dependencies (do not commit local gems or lock files)
.bundle/         # Bundler's local gem storage
vendor/          # Gems installed by Bundler (if using vendor/bundle)
Gemfile.lock     # Lock file for Ruby dependencies (optional to commit; see note below)
